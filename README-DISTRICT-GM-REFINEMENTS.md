# District GM refinements

Replace these files:

- `src/App.jsx`
- `src/styles/district-basketball-lab.css`

Changes:

1. Adds `max-width: 300px` to `.lineup-editor-list-image` and its `img`.
2. Removes the Cards view image `height: 80%` override.
3. Replaces the stacked `Stats` label with a compact inline `Stat View | dropdown` control.
