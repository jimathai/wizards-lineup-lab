# Lineup Editor persistence

1. Run `supabase/lineup_editor_persistence.sql` in Supabase SQL Editor.
2. Copy the two `src` files into the project.
3. Restart Vite.
4. Change the current five, wait one second, then refresh.

The current editor five and formation are stored separately from the six
saved-lineup slots.
