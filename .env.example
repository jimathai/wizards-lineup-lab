# Copy this file to .env.local and replace the placeholder with your key.
# Never commit .env.local.
VITE_BDL_API_KEY=your_api_key_here

# Player data source. Keep local until an external provider is enabled.
VITE_PLAYER_DATA_PROVIDER=local
