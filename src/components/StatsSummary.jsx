export default function StatsSummary({players,statMode}){const t=players.reduce((a,p)=>{const s=p?.[statMode];if(s){a.pts+=s.pts;a.reb+=s.reb;a.ast+=s.ast;a.stocks+=(s.stl||0)+(s.blk||0)}return a},{pts:0,reb:0,ast:0,stocks:0});const items=[['Rotation PTS',t.pts],['Rotation REB',t.reb],['Rotation AST',t.ast],['Rotation STOCKS',t.stocks],['Players',players.length]];return <section className="summary-grid">{items.map(([l,v])=><div className="summary-card" key={l}><span>{l}</span><strong>{l==='Players'?v:v.toFixed(1)}</strong></div>)}</section>}