import PlayerCard from './PlayerCard'
export default function DepthUnit({title,players,statMode,onDrop,onRemove,onOpenPicker,onSelectPlayer}){return <section className="compact-depth-unit"><div className="depth-unit-head"><h2>{title}</h2></div><div className="compact-depth-grid">{players.map((player,index)=><div key={index} className="compact-depth-slot" onDragOver={(e)=>e.preventDefault()} onDrop={(e)=>{const id=Number(e.dataTransfer.getData('player-id'));if(id)onDrop(index,id)}}>{player?<PlayerCard player={player} statMode={statMode} onRemove={()=>onRemove(index)} onSelect={onSelectPlayer}/>:<button className="compact-depth-plus" onClick={(e)=>onOpenPicker(index,e.currentTarget.getBoundingClientRect())}>+</button>}</div>)}</div></section>}
